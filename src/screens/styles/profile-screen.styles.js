import { StyleSheet } from "react-native";
import {
  BORDERRADIUS,
  COLORS,
  FONTFAMILY,
  FONTSIZE,
  SPACING,
} from "../../theme/theme";

export const styles = StyleSheet.create({
  ScreenContainer: {
    flex: 1,
    alignItems: "center",
  },
});